declare const __DEFAULT_REPO_ROOT__: string;

export const DEFAULT_REPO_ROOT = __DEFAULT_REPO_ROOT__;
export const DEFAULT_GRAPH_PATH = `${DEFAULT_REPO_ROOT}/.understand-anything/knowledge-graph.json`;
